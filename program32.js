// write a prgm to find the sum of two numbers using functions


function sum(a,b){
    const sum = a + b;
    // console.log(`sum of ${a} and ${b} is ${sum}`);
    return sum;
}

console.log(sum(3,7));

sum(2,4);

sum(10,6);

sum(3,4);

sum(1,5);